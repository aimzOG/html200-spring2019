function numberMultiplier() {
  let inputA = prompt('Enter a number: ');
  let inputB = prompt('Enter another number: ');
  let result = 0;

  result = Number(inputA) * Number(inputB);

  alert(result);
}
